/** @type {import('tailwindcss').Config} */

module.exports = {
    darkMode: 'class',
    content: [
      // add other paths if needed
    ],
    theme: {
      extend: {},
    },
    plugins: [],
}