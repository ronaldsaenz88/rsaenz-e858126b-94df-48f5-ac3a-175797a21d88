/** @type {import('tailwindcss').Config} */

module.exports = {
    darkMode: 'class',
    content: [
      "./apps/dashboard/src/**/*.{html,ts}",
      // add other paths if needed
    ],
    theme: {
      extend: {},
    },
    plugins: [],
}